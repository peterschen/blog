apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: mssql
  namespace: pass-standalone
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 10Gi
  storageClassName: pd-balanced